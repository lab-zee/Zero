# Archived migrations (pre-baseline)

These are the 17 migrations from before the schema was consolidated onto a single
Alembic baseline. They are kept for historical reference only.

**Alembic does not load them** — they live outside `alembic/versions/`, so they are not
part of the active migration chain. Do not move them back.

See `backend/MIGRATIONS.md` for how the schema is managed now.
